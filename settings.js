const settings = {
  // src folder
  src: {
    source: __dirname + '/src/', // source folder
    images: __dirname + '/src/img/', // images
    sass: __dirname + '/src/sass/', // sass
  },

  // dist folder
  dist: {
    css: __dirname + '/dist/css/', // stylesheets
    js: __dirname + '/dist/js/', // javascripts
    images: __dirname + '/dist/img/', // images
  },
};

module.exports = settings;
